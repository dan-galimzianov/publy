providers = ["node"]

[phases.install]
cmds = ["npm ci --prefix backend"]

[phases.build]
cmds = ["npm run build --prefix backend", "npx prisma generate --prefix backend", "npx prisma db push --prefix backend"]

[start]
cmd = "npm start --prefix backend"
