providers = ["node"]

[phases.install]
cmds = ["npm ci --prefix backend"]

[phases.build]
cmds = ["cd backend && npx prisma generate", "npm run build --prefix backend"]

[start]
cmd = "cd backend && npx prisma db push && npm start"
